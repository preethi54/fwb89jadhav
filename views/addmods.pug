//- extends layout

//- block content
//-   h1= title
//-   p Welcome to #{title}
//-   p No. of rows:#{query.rows}
//-   p No. of columns:#{query.cols}
//-   - var rows = Number(query.rows)
//-   - var cols = Number(query.cols)
//-   if !(Number.isInteger(rows)) ||  !(Number.isInteger(rows))
//-     p not integer numbers 
//-   else if rows > 6 
//-     p rows not in range 1-6
//-   else if cols > 6
//-     p cols not in range 1-6 
//-   else 
//-     table#outer 
//-       - for (let i=0;i<query.rows;i++)
//-         tr#middle
//-           - for (let j=0;i<query.cols;j++)
//-             td=i+j
//-           -}
//-       -}
extends layout

block content
 h1= title
 p No. of Rows:#{query.rows}
 p No. of Columns:#{query.cols}
 - var rows=Number(query.rows)
 - var cols=Number(query.cols)
 if !(Number.isInteger(rows)) ||  !(Number.isInteger(rows))
  p Given values are not integers 
 else if rows > 6
  p Number of rows should be less than 6
 else if cols > 6
  p Number of columns should be less than 6
 else
  table#outer
   - for(let i=0; i<query.rows; i++) {
    tr#middle
     - for(let j=0; j<query.cols; j++) {
      td= i+j
     -}
   -}